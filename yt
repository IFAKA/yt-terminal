#!/bin/bash
# YouTube terminal browser with detailed progress and thumbnails

set -o pipefail

VERSION="1.0.0"

# Default configuration
DEFAULT_NUM_RESULTS=10
DEFAULT_MAX_QUALITY=1080
DEFAULT_TIMEOUT=90
DEFAULT_VOLUME=100
DEFAULT_CACHE_DIR="$HOME/.cache/yt-thumbnails"
CONFIG_DIR="$HOME/.config/yt"
CONFIG_FILE="$CONFIG_DIR/config"

# Load configuration
load_config() {
    # Set defaults
    NUM_RESULTS=${NUM_RESULTS:-$DEFAULT_NUM_RESULTS}
    MAX_QUALITY=${MAX_QUALITY:-$DEFAULT_MAX_QUALITY}
    TIMEOUT=${TIMEOUT:-$DEFAULT_TIMEOUT}
    VOLUME=${VOLUME:-$DEFAULT_VOLUME}
    CACHE_DIR=${CACHE_DIR:-$DEFAULT_CACHE_DIR}
    
    # Load user config if it exists
    if [ -f "$CONFIG_FILE" ]; then
        # shellcheck source=/dev/null
        source "$CONFIG_FILE"
    fi
}

# Cleanup function for temp files
cleanup() {
    local exit_code=$?
    if [ -n "$tmpfile" ] && [ -f "$tmpfile" ]; then
        rm -f "$tmpfile"
    fi
    exit "$exit_code"
}

trap cleanup EXIT INT TERM

# Check dependencies
check_dependencies() {
    local missing_deps=()
    local optional_deps=()
    
    # Required dependencies
    command -v yt-dlp &> /dev/null || missing_deps+=("yt-dlp")
    command -v mpv &> /dev/null || missing_deps+=("mpv")
    command -v fzf &> /dev/null || missing_deps+=("fzf")
    command -v jq &> /dev/null || missing_deps+=("jq")
    command -v curl &> /dev/null || missing_deps+=("curl")
    
    # Optional dependencies
    command -v chafa &> /dev/null || optional_deps+=("chafa")
    
    if [ ${#missing_deps[@]} -ne 0 ]; then
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "❌ Missing required dependencies"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        echo "Please install the following:"
        for dep in "${missing_deps[@]}"; do
            echo "  • $dep"
        done
        echo ""
        echo "Installation command (macOS):"
        echo "  brew install ${missing_deps[*]}"
        echo ""
        echo "Installation command (Linux/Debian):"
        echo "  sudo apt install ${missing_deps[*]}"
        echo ""
        return 1
    fi
    
    if [ ${#optional_deps[@]} -ne 0 ]; then
        echo "ℹ️  Optional: Install 'chafa' for thumbnail previews"
        echo "   brew install chafa"
        echo ""
    fi
    
    return 0
}

# Show usage information
show_usage() {
    echo "Usage: yt [OPTIONS] <search term>"
    echo ""
    echo "Options:"
    echo "  -f, --first       Auto-play first search result"
    echo "  -a, --audio-only  Play audio only (no video)"
    echo "  -p, --playlist    Search for playlists instead of videos"
    echo ""
    echo "Examples:"
    echo "  yt vim tutorial              # Search and select interactively"
    echo "  yt -f lofi hip hop           # Auto-play first result"
    echo "  yt -a music                  # Search and play audio only"
    echo "  yt -p lofi playlists         # Browse playlists"
    echo "  yt -a -p chill music         # Browse playlists, play audio only"
    echo "  yt <url>                     # Play video from URL"
    echo ""
    echo "Version: $VERSION"
    echo "Configuration file: $CONFIG_FILE"
}

# Play video from URL
play_url() {
    local url="$1"
    local audio_only="$2"
    local format
    
    if [ "$audio_only" = "true" ]; then
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "🎵 Playing audio from URL..."
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        format="bestaudio/best"
    else
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "▶ Playing video from URL..."
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        format="bestvideo[height<=${MAX_QUALITY}]+bestaudio/best"
    fi
    echo ""
    
    if ! mpv "$url" \
        --ytdl-format="$format" \
        --volume="$VOLUME" \
        --audio-device=auto; then
        echo "❌ Error playing video"
        return 1
    fi
    
    return 0
}

# Search YouTube
search_youtube() {
    local search_term="$1"
    local search_playlists="$2"
    
    # For playlist mode, automatically append " playlist" to help find playlist videos
    if [ "$search_playlists" = "true" ]; then
        search_term="$search_term playlist"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "📚 Searching for: $search_term"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        echo "ℹ️  Tip: Select a playlist video - the entire playlist will play!"
        echo ""
    else
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "🔍 Searching YouTube for: $search_term"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
    fi
    
    # Create temp file
    tmpfile=$(mktemp) || { echo "❌ Failed to create temp file"; return 1; }
    
    echo "⏳ Fetching videos..."
    
    # Create temp file for raw JSON
    local jsonfile
    jsonfile=$(mktemp) || { echo "❌ Failed to create temp file"; return 1; }
    
    # Fetch videos to temp file
    if ! timeout "$TIMEOUT" yt-dlp "ytsearch${NUM_RESULTS}:$search_term" \
        --dump-json \
        --skip-download \
        --no-warnings \
        --quiet \
        2>/dev/null > "$jsonfile"; then
        rm -f "$jsonfile"
        echo "❌ Search failed or timed out"
        return 1
    fi
    
    # Parse and display results
    local count=0
    while IFS= read -r line && [ "$count" -lt "$NUM_RESULTS" ]; do
        # Parse video data
        video_data=$(echo "$line" | jq -r '"\(.title)\t\(.duration_string // "LIVE")\t\(.id)\t\(.thumbnail)"')
        echo "$video_data" >> "$tmpfile"
        
        count=$((count + 1))
        echo "  ✓ Found ($count/$NUM_RESULTS): $(echo "$line" | jq -r '.title' | cut -c1-60)..."
    done < "$jsonfile"
    
    rm -f "$jsonfile"
    echo ""
    
    # Check if we got any results
    if [ ! -s "$tmpfile" ] || [ "$count" -eq 0 ]; then
        echo "❌ No results found!"
        return 1
    fi
    
    return 0
}

# Download thumbnails
download_thumbnails() {
    local search_playlists="$1"
    mkdir -p "$CACHE_DIR" || { echo "❌ Failed to create cache directory"; return 1; }
    
    while IFS=$'\t' read -r title duration id thumbnail; do
        # Format result line - always use same format since playlist mode now searches for videos
        results="${results}$(printf "%-80s [%8s] %s" "$title" "$duration" "$id")"$'\n'
        
        # Download thumbnail in background for preview
        if [ -n "$thumbnail" ]; then
            curl -s "$thumbnail" -o "$CACHE_DIR/$id.jpg" 2>/dev/null &
        fi
    done < "$tmpfile"
    
    if [ -z "$results" ]; then
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "❌ No results found!"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        return 1
    fi
    
    # Count actual non-empty lines
    items_count=$(echo "$results" | grep -c '^.')
    echo "✅ Successfully fetched $items_count videos!"
    echo "📥 Downloading thumbnails..."
    echo ""
    
    # Wait a bit for thumbnails to download
    sleep 2
    echo "✅ Ready!"
    sleep 1
    
    return 0
}

# Preview playlist contents for a video (for fzf preview)
preview_playlist() {
    local video_id="$1"
    
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "📚 Checking for Playlist..."
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
    echo "⏳ Loading..."
    echo ""
    
    # Try to get playlist info from the video
    local playlist_info
    playlist_info=$(timeout 8 yt-dlp "https://youtube.com/watch?v=$video_id" \
        --dump-json \
        --skip-download \
        --no-warnings \
        --quiet \
        2>/dev/null | jq -r 'if .playlist_id then "\(.playlist_id)\t\(.playlist_title // "Playlist")\t\(.playlist_count // "?")" else empty end')
    
    if [ -z "$playlist_info" ]; then
        echo "ℹ️  No playlist found"
        echo ""
        echo "This video is not part of a playlist."
        echo "It will play as a single video."
        return 0
    fi
    
    local playlist_id playlist_title playlist_count
    IFS=$'\t' read -r playlist_id playlist_title playlist_count <<< "$playlist_info"
    
    echo "✅ Found Playlist!"
    echo ""
    echo "Title: $playlist_title"
    echo "Videos: $playlist_count"
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "The entire playlist will play when selected"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
}

# Play entire playlist
play_playlist() {
    local playlist_id="$1"
    local playlist_title="$2"
    local audio_only="$3"
    local format
    local url
    
    url="https://youtube.com/playlist?list=$playlist_id"
    
    # Set format based on audio-only flag
    if [ "$audio_only" = "true" ]; then
        format="bestaudio/best"
    else
        format="bestvideo[height<=${MAX_QUALITY}]+bestaudio/best"
    fi
    
    clear
    if [ "$audio_only" = "true" ]; then
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "🎵 PLAYING PLAYLIST (AUDIO ONLY)"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    else
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "📚 PLAYING PLAYLIST"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    fi
    echo ""
    echo "Playlist: $playlist_title"
    echo "URL:      $url"
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "MPV Settings:"
    echo "  - Format: $format"
    echo "  - Volume: $VOLUME"
    echo "  - Audio Device: auto"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
    echo "Starting playlist playback in 2 seconds..."
    sleep 2
    
    # Play playlist with mpv
    if ! mpv "$url" \
        --ytdl-format="$format" \
        --volume="$VOLUME" \
        --audio-device=auto; then
        echo ""
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "❌ Playback failed"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        return 1
    fi
    
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "✅ Playlist finished"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    
    return 0
}

# Select video with fzf or return first result
select_video() {
    local search_term="$1"
    local auto_first="$2"
    local search_playlists="$3"
    local videos_count
    local selected
    
    # Count actual non-empty lines
    videos_count=$(echo "$results" | grep -c '^.')
    
    # If auto-first flag is set, return first result
    if [ "$auto_first" = "true" ]; then
        selected=$(echo "$results" | head -n 1)
        if [ -z "$selected" ]; then
            echo "❌ No results found" >&2
            return 1
        fi
        echo "🎯 Auto-playing first result..." >&2
        echo "" >&2
        echo "$selected"
        return 0
    fi
    
    # Clear screen and show results (redirect to stderr to avoid capture)
    clear >&2
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
    if [ "$search_playlists" = "true" ]; then
        echo "📚 Playlist Mode: $search_term ($videos_count results)" >&2
    else
        echo "🎥 YouTube Search: $search_term ($videos_count results)" >&2
    fi
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
    echo "" >&2
    
    # Different preview based on search mode
    if [ "$search_playlists" = "true" ]; then
        # For playlist mode, show if video has an associated playlist
        selected=$(echo "$results" | fzf \
            --height=90% \
            --layout=reverse \
            --border \
            --prompt="▶ " \
            --header="[j/k] navigate | [Ctrl-d/u] page | [Enter] check & play | [q] quit" \
            --preview="$0 --preview-playlist \$(echo {} | awk '{print \$NF}')" \
            --preview-window=right:50%:wrap \
            --color='fg:#ffffff,bg:#000000,hl:#ff0000,fg+:#ffffff,bg+:#333333,hl+:#ff0000' \
            --pointer='▶' \
            --marker='✓' \
            --bind 'j:down,k:up' \
            --bind 'ctrl-d:half-page-down,ctrl-u:half-page-up' \
            --bind 'ctrl-f:page-down,ctrl-b:page-up' \
            --bind 'q:abort')
    else
        # For videos, show thumbnail
        selected=$(echo "$results" | fzf \
            --height=90% \
            --layout=reverse \
            --border \
            --prompt="▶ " \
            --header="[j/k] navigate | [Ctrl-d/u] page | [Enter] play | [q] quit" \
            --preview="video_id=\$(echo {} | awk '{print \$NF}'); if [ -f \"$CACHE_DIR/\$video_id.jpg\" ]; then chafa --format=kitty --size=60x30 \"$CACHE_DIR/\$video_id.jpg\" 2>/dev/null; else echo '⏳ Downloading thumbnail...'; fi" \
            --preview-window=right:40%:wrap \
            --color='fg:#ffffff,bg:#000000,hl:#ff0000,fg+:#ffffff,bg+:#333333,hl+:#ff0000' \
            --pointer='▶' \
            --marker='✓' \
            --bind 'j:down,k:up' \
            --bind 'ctrl-d:half-page-down,ctrl-u:half-page-up' \
            --bind 'ctrl-f:page-down,ctrl-b:page-up' \
            --bind 'q:abort')
    fi
    
    if [ -z "$selected" ]; then
        echo "" >&2
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
        echo "❌ Cancelled" >&2
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" >&2
        return 1
    fi
    
    echo "$selected"
    return 0
}

# Play selected video
play_video() {
    local selected="$1"
    local audio_only="$2"
    local video_id title url format
    
    video_id=$(echo "$selected" | awk '{print $NF}')
    title=$(echo "$selected" | sed 's/\[.*\].*$//' | xargs)
    url="https://youtube.com/watch?v=$video_id"
    
    # Set format based on audio-only flag
    if [ "$audio_only" = "true" ]; then
        format="bestaudio/best"
    else
        format="bestvideo[height<=${MAX_QUALITY}]+bestaudio/best"
    fi
    
    # Show thumbnail before playing
    clear
    if command -v chafa &> /dev/null && [ -f "$CACHE_DIR/$video_id.jpg" ]; then
        if [ "$audio_only" = "true" ]; then
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "🎵 AUDIO PREVIEW"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        else
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "▶ VIDEO PREVIEW"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        fi
        echo ""
        chafa --format=kitty --size=60x30 "$CACHE_DIR/$video_id.jpg" 2>/dev/null
        echo ""
    fi
    
    if [ "$audio_only" = "true" ]; then
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "🎵 PLAYING AUDIO"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    else
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "▶ PLAYING"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    fi
    echo ""
    echo "Title:    $title"
    echo "Video ID: $video_id"
    echo "URL:      $url"
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "MPV Settings:"
    echo "  - Format: $format"
    echo "  - Volume: $VOLUME"
    echo "  - Audio Device: auto"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo ""
    echo "Starting playback in 2 seconds..."
    sleep 2
    
    # Play with explicit settings
    if ! mpv "$url" \
        --ytdl-format="$format" \
        --volume="$VOLUME" \
        --audio-device=auto; then
        echo ""
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "❌ Playback failed"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        return 1
    fi
    
    echo ""
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    echo "✅ Playback finished"
    echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
    
    return 0
}

# Main function
main() {
    # Load configuration
    load_config
    
    # Handle internal preview mode (called by fzf)
    if [ "$1" = "--preview-playlist" ]; then
        preview_playlist "$2"
        exit 0
    fi
    
    # Parse flags
    local auto_first=false
    local audio_only=false
    local search_playlists=false
    local search_term=""
    
    while [[ $# -gt 0 ]]; do
        case "$1" in
            -f|--first)
                auto_first=true
                shift
                ;;
            -a|--audio-only)
                audio_only=true
                shift
                ;;
            -p|--playlist)
                search_playlists=true
                shift
                ;;
            -h|--help)
                show_usage
                exit 0
                ;;
            -*)
                echo "Unknown option: $1"
                show_usage
                exit 1
                ;;
            *)
                search_term="$search_term $1"
                shift
                ;;
        esac
    done
    
    # Trim leading/trailing whitespace
    search_term=$(echo "$search_term" | xargs)
    
    # Check dependencies (always run before usage to give helpful error messages)
    if ! check_dependencies; then
        exit 1
    fi
    
    if [ -z "$search_term" ]; then
        show_usage
        exit 1
    fi
    
    # Check if input is a URL
    if [[ "$search_term" =~ ^https?:// ]]; then
        if ! play_url "$search_term" "$audio_only"; then
            exit 1
        fi
        exit 0
    fi
    
    # Search YouTube
    if ! search_youtube "$search_term" "$search_playlists"; then
        exit 1
    fi
    
    # Download thumbnails and format results
    if ! download_thumbnails "$search_playlists"; then
        exit 1
    fi
    
    # Select video or playlist
    local selected
    if ! selected=$(select_video "$search_term" "$auto_first" "$search_playlists"); then
        exit 0
    fi
    
    # If playlist mode, try to find and play the playlist containing this video
    if [ "$search_playlists" = "true" ]; then
        local video_id video_title
        video_id=$(echo "$selected" | awk '{print $NF}')
        video_title=$(echo "$selected" | sed 's/\[.*\].*$//' | xargs)
        
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "📚 Looking for playlist containing this video..."
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        
        # Try to get playlist ID from the video
        local playlist_info
        playlist_info=$(timeout 15 yt-dlp "https://youtube.com/watch?v=$video_id" \
            --dump-json \
            --skip-download \
            --no-warnings \
            --quiet \
            2>/dev/null | jq -r 'if .playlist_id then "\(.playlist_id)\t\(.playlist_title // "Playlist")\t\(.playlist_count // "?")" else empty end')
        
        if [ -n "$playlist_info" ]; then
            local playlist_id playlist_title playlist_count
            IFS=$'\t' read -r playlist_id playlist_title playlist_count <<< "$playlist_info"
            
            echo "✅ Found playlist: $playlist_title ($playlist_count videos)"
            echo ""
            
            if ! play_playlist "$playlist_id" "$playlist_title" "$audio_only"; then
                exit 1
            fi
        else
            echo "ℹ️  No playlist found for this video, playing single video instead..."
            echo ""
            sleep 2
            
            if ! play_video "$selected" "$audio_only"; then
                exit 1
            fi
        fi
    else
        # Play single video
        if ! play_video "$selected" "$audio_only"; then
            exit 1
        fi
    fi
    
    exit 0
}

# Run main function
main "$@"
